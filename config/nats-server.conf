# Static NATS auth config.
# Operator/account JWT artifacts are generated under .nats/ by
# scripts/provision-nats-identities.sh and mounted into /data.

jetstream {
  store_dir: "/data/jetstream"
}

operator: "/data/resolver/lifeos-operator.jwt"
resolver: {
  type: full
  dir: "/data/resolver"
}
