# Ambient profile: multimodal plus room awareness + homesteading-lite.
profile: ambient

services:
  secretsService:
    host: secrets-service
    port: 4101
  serviceCatalog:
    host: service-catalog
    port: 4102
  featureFlagService:
    host: feature-flag-service
    port: 4103
  moduleLoader:
    host: module-loader
    port: 4104

modules:
  enabled:
    - reasoning-engine
    - goal-engine
    - calendar
    - fitness
    - voice
    - vision-ingestion-lite
    - room-awareness
    - homesteading-lite
