# ADR-005: Phase 6 Home-Node Architecture

## Status

Accepted

## Context

Phase 6 introduces an always-on home-node runtime layer for ambient household surfaces. This ADR captures and freezes architectural decisions that are already implemented in the running code so contributors can extend Phase 6 behavior without reverse-engineering service boundaries, trust-policy rules, or voice/session lifecycle behavior.

## Decision

| Decision                                                                                                                                                                                                                                                                                                                                                                                                | Code evidence                                                                                                                                           |
| ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Three-layer model (Home Node Runtime / Core Platform / Ambient Surfaces)                                                                                                                                                                                                                                                                                                                                | `services/home-node`, `services/dashboard`, `apps/home-display`, `apps/mobile`                                                                          |
| `home_id` is resolved dynamically via `HomeNodeGraphClient.getHomeByHouseholdId(householdId)` in both `publishSnapshotUpdatedEvent()` and `publishDisplayFeedUpdatedEvent()`; if no home is found for the household, the event is not published and a warning is logged                                                                                                                                 | `services/home-node/src/app.ts`                                                                                                                         |
| Surface trust levels: `personal`, `household`, `guest`; immutable once registered                                                                                                                                                                                                                                                                                                                       | `packages/home-node-core/src/client.ts` (`registerSurface()` trust-level immutability check), `services/home-node/src/feed.ts` (`applyContentFilter()`) |
| Content filtering by trust level: `personal` full feed; `household` strips sensitive reminders; `guest` empties events/chores/reminders and only shows non-warning notices and shopping items                                                                                                                                                                                                           | `services/home-node/src/feed.ts` (`applyContentFilter()`)                                                                                               |
| `guest_mode` overrides surface trust level to effective `guest`                                                                                                                                                                                                                                                                                                                                         | `services/home-node/src/feed.ts` (`resolveEffectiveTrustLevel()`)                                                                                       |
| Home-mode state machine modes: `home`, `away`, `morning_routine`, `evening_routine`, `quiet_hours`, `guest_mode`; transitions driven by `presence.anyone_home`, `routine.morning`, `routine.evening`, `quiet_hours`; consent gate on each transition                                                                                                                                                    | `packages/home-node-core/src/client.ts` (`resolveHomeModeTransition()`, `buildNextSnapshot()`)                                                          |
| `buildNotices()` rules: `quiet_hours` -> info notice; `guest_mode` -> warning notice; `adapter_health !== 'healthy'` -> warning notice                                                                                                                                                                                                                                                                  | `services/home-node/src/feed.ts` (`buildNotices()`)                                                                                                     |
| Display feed cache TTL is 30 seconds per surface with stale fallback on dashboard fetch failure                                                                                                                                                                                                                                                                                                         | `services/home-node/src/feed.ts` (`DISPLAY_FEED_CACHE_TTL_MS = 30_000`, `DisplayFeedAggregator.getDisplayFeed()`)                                       |
| Voice data retention defaults to disabled (`retainAudio = false`), configurable via `LIFEOS_HOME_NODE_VOICE_RETAIN_AUDIO`; when disabled, capture event stores `audioRef = null`                                                                                                                                                                                                                        | `services/home-node/src/app.ts` (retain-audio env handling), `packages/home-node-core/src/voice-session.ts` (`retainSessionAudio()`)                    |
| Voice session TTL defaults to 60 seconds, configurable via `LIFEOS_HOME_NODE_VOICE_SESSION_TTL_MS`; expired sessions fail with reason `expired`                                                                                                                                                                                                                                                         | `packages/home-node-core/src/voice-session.ts` (`DEFAULT_SESSION_TTL_MS`, `expireSessions()`)                                                           |
| Voice quiet-hours gate fails sessions started while quiet hours are active with reason `quiet_hours`                                                                                                                                                                                                                                                                                                    | `packages/home-node-core/src/voice-session.ts` (`startSession()`)                                                                                       |
| `deriveTargetHint()` routing maps transcript keywords to `shopping`, `chore`, `reminder`, `note`, or `unknown`                                                                                                                                                                                                                                                                                          | `packages/home-node-core/src/voice-session.ts` (`deriveTargetHint()`)                                                                                   |
| Watchdog interval is 60 seconds and checks event-bus health, stale surface heartbeats (5-minute inactivity threshold), voice runtime health, and session expiry                                                                                                                                                                                                                                         | `services/home-node/src/app.ts` (`WATCHDOG_INTERVAL_MS`, `SURFACE_INACTIVITY_THRESHOLD_MS`, watchdog `setInterval`)                                     |
| Ambient action audit log appends every home-state change and voice capture to `ambient_actions` via `appendAmbientAction()`                                                                                                                                                                                                                                                                             | `services/home-node/src/app.ts` (`handleHomeStateChangedEvent()`, `handleVoiceCaptureEvent()`)                                                          |
| `POST /api/home-node/homes` and `POST /api/home-node/zones` are surface-secret-protected provisioning routes; they require `x-lifeos-surface-secret` (same auth class as `/surfaces/register`, `/surfaces/:surfaceId/heartbeat`, and `DELETE /surfaces/:surfaceId`). `apps/home-display` already sends this header via `createHeaders()` in `useDisplayFeed.ts`; the server-side gate was added in SQ-7 | `services/home-node/src/routes.ts`, `apps/home-display/src/hooks/useDisplayFeed.ts`                                                                     |
| Surface kinds are frozen to `kitchen_display`, `hallway_display`, `living_room_display`, `desk_display`, `voice_endpoint`, `mobile_app`                                                                                                                                                                                                                                                                 | `apps/home-display/src/types.ts` (`SurfaceKind`)                                                                                                        |
| Display modes are `kitchen` and `hallway`                                                                                                                                                                                                                                                                                                                                                               | `apps/home-display/src/types.ts` (`DisplayMode`)                                                                                                        |
| Dashboard base URL defaults to `http://localhost:3000` and is configurable through `LIFEOS_DASHBOARD_BASE_URL`                                                                                                                                                                                                                                                                                          | `services/home-node/src/feed.ts` (`normalizeDashboardBaseUrl()`)                                                                                        |

## Consequences

- Multi-home support is now live: `home_id` is DB-resolved per household at event-publish time; no hardcoded fallback remains.
- The `home-state` module does not publish `homeNodeStateSnapshotUpdated` — the home-node service is the sole publisher, and it is the only place where `home_id` is resolved and emitted.
- Surface trust level is immutable after registration, so role/trust corrections require explicit deregistration and re-registration.
- All mutating home-node routes now uniformly require the surface secret.
- Phase 6 contributors must align feed filtering, notice semantics, and voice/session behavior with the frozen decisions above.

## Route Auth Classification

| Route | Method | Auth class |
| ----- | ------ | ---------- |
| `/api/home-node/homes` | POST | `surface-secret` |
| `/api/home-node/zones` | POST | `surface-secret` |
| `/api/home-node/snapshot/:householdId` | GET | `surface-secret` |
| `/api/home-node/surfaces` | GET | `surface-secret` |
| `/api/home-node/surfaces/register` | POST | `surface-secret` |
| `/api/home-node/surfaces` | POST | `surface-secret` |
| `/api/home-node/surfaces/:surfaceId` | DELETE | `surface-secret` |
| `/api/home-node/surfaces/:surfaceId/heartbeat` | POST | `surface-secret` |
| `/api/home-node/display-feed/:surfaceId` | GET | `surface-secret` |
| `/api/home-node/display-feed-hints/:surfaceId` | GET | `surface-secret` |
| `/health/live` | GET | `public` |
| `/health/ready` | GET | `public` |

Bearer auth for GET routes is deferred to the auth-unification wave. `/health/*` endpoints must remain unauthenticated regardless of `enforceRouteAuthMode`.

## Alternatives Considered

- Dynamic trust-level mutation after registration was rejected; immutability is enforced in `registerSurface()`.
- Raw audio retention by default (opt-out) was rejected; retention is opt-in through `LIFEOS_HOME_NODE_VOICE_RETAIN_AUDIO`.
